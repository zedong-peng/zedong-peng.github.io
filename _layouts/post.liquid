---
layout: default
---
{% assign year = page.date | date: '%Y' %}
{% assign tags = page.tags | join: '' %}
{% assign categories = page.categories | join: '' %}
{% assign url_beginning = page.url | slice: 0, 6 %}
{% assign page_language = page.lang | default: site.lang %}
{% assign translations = '' | split: ',' %}
{% if page.translation_key %}
  {% assign translations = site.posts | where: 'translation_key', page.translation_key %}
{% endif %}

{% if page._styles %}
  <!-- Page/Post style -->
  <style type="text/css">
    {{ page._styles }}
  </style>
{% endif %}

<div class="post">
  <header class="post-header">
    <h1 class="post-title">{{ page.title }}</h1>
    <p class="post-meta">
      {% if page_language contains 'zh' %}
        发布于 {{ page.date | date: '%Y年%m月%d日' }}
        {% if page.author %}，作者：{{ page.author }}{% endif %}
        {% if page.last_updated %}，最后更新于 {{ page.last_updated | date: '%Y年%m月%d日' }}{% endif %}
      {% else %}
        Created on {{ page.date | date: '%B %d, %Y' }}
        {% if page.author %}by {{ page.author }}{% endif %}
        {% if page.last_updated %}, last updated on {{ page.last_updated | date: '%B %d, %Y' }}{% endif %}
      {% endif %}
      {% if page.meta %}• {{ page.meta }}{% endif %}
    </p>
    <p class="post-tags">
      {% if url_beginning == '/blog/' %}
        <a href="{{ year | prepend: '/blog/' | relative_url }}"> <i class="fa-solid fa-calendar fa-sm"></i> {{ year }} </a>
      {% else %}
        <i class="fa-solid fa-calendar fa-sm"></i> {{ year }}
      {% endif %}
      {% if tags != '' %}
        &nbsp; &middot; &nbsp;
        {% for tag in page.tags %}
          {% if url_beginning == '/blog/' %}
            <a href="{{ tag | slugify | prepend: '/blog/tag/' | relative_url }}"> <i class="fa-solid fa-hashtag fa-sm"></i> {{ tag }}</a>
          {% else %}
            <i class="fa-solid fa-hashtag fa-sm"></i> {{ tag }}
          {% endif %}
          {% unless forloop.last %}
            &nbsp;
          {% endunless %}
        {% endfor %}
      {% endif %}

      {% if categories != '' %}
        &nbsp; &middot; &nbsp;
        {% for category in page.categories %}
          {% if url_beginning == '/blog/' %}
            <a href="{{ category | slugify | prepend: '/blog/category/' | relative_url }}"> <i class="fa-solid fa-tag fa-sm"></i> {{ category -}}</a>
          {% else %}
            <i class="fa-solid fa-tag fa-sm"></i> {{ category }}
          {% endif %}
          {% unless forloop.last %}
            &nbsp;
          {% endunless %}
        {% endfor %}
      {% endif %}
    </p>
    {% if page.translation_key and translations.size > 1 %}
      <nav class="post-language-switcher" aria-label="Article language">
        <div class="btn-group" role="group">
          {% for translation in translations %}
            {% assign translation_language = translation.lang | default: site.lang %}
            <a
              class="btn btn-sm {% if translation.url == page.url %}btn-primary{% else %}btn-outline-primary{% endif %}"
              href="{{ translation.url | relative_url }}"
              lang="{{ translation_language }}"
              hreflang="{{ translation_language }}"
              {% if translation.url == page.url %}
                aria-current="page"
              {% endif %}
            >
              {% if translation_language contains 'zh' %}
                中文
              {% elsif translation_language == 'en' %}
                English
              {% else %}
                {{ translation_language }}
              {% endif %}
            </a>
          {% endfor %}
        </div>
      </nav>
    {% endif %}
  </header>

  <article class="post-content">
    {% if page.toc and page.toc.beginning %}
      <div id="table-of-contents">
        {% toc %}
      </div>
      <hr>
    {% endif %}
    <div id="markdown-content">
      {{ content }}
    </div>
  </article>

  {% if page.citation %}
    {% include citation.liquid %}
  {% endif %}

  {% if page.related_publications %}
    <h2>References</h2>
    <div class="publications">
      {% bibliography --group_by none --cited_in_order %}
    </div>
  {% endif %}

  {% if site.disqus_shortname and page.disqus_comments %}
    {% include disqus.liquid %}
  {% endif %}
  {% if site.giscus and page.giscus_comments %}
    {% include giscus.liquid %}
  {% endif %}
</div>
